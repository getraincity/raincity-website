node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
.env*.local
next-env.d.ts
.vercel
*.tsbuildinfo

# Section-capture screenshots from the shot-*.mjs scripts, written to the
# project root. Not build output or source — local review artifacts only.
/*.png
# shot-part.mjs takes an output directory; this is the usual one.
/shots/

# Claude Code ephemeral worktrees — created and discarded per session.
# Run `git worktree list` to see any that are still registered; remove with
# `git worktree remove <path>` then `git worktree prune`.
.claude/worktrees/

# Audit documents generated by Claude Code review sessions. These are
# development artifacts and should not be committed to the project record
# unless intentionally promoted. Promote by removing the entry below.
senior-developer-audit.md
senior-developer-audit-fixes-report.md
seo-expert-audit.md
brand-design-audit.md
